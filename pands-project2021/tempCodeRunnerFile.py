def species():
    with open (data) as f:
    z = (f.pd.DataFrame(data, columns = ['class'])
    colors = []
    for z in species:
        if (z == 'setosa'):
        colors.append(0)
        if (z == 'versicolor'):
        colors.append(1)
        if (z == 'virginica'):
        colors.append(2)
return colors